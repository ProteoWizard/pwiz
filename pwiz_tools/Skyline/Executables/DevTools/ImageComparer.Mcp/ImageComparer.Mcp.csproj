<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <RootNamespace>ImageComparer.Mcp</RootNamespace>
    <AssemblyName>ImageComparer.Mcp</AssemblyName>
    <LangVersion>latest</LangVersion>
    <!-- Windows-only for System.Drawing.Common GDI+ support -->
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
  </PropertyGroup>

  <ItemGroup>
    <!-- No stable release available yet; update when one ships -->
    <PackageReference Include="ModelContextProtocol" Version="0.8.0-preview.1" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="8.0.1" />
    <PackageReference Include="System.Drawing.Common" Version="8.0.10" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ImageComparer.Core\ImageComparer.Core.csproj" />
  </ItemGroup>

</Project>
