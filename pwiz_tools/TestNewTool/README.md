# TestNewTool

Placeholder directory added to demonstrate the build dependency graph review rule.

This directory has no specific entry in `scripts/misc/vcs_trigger_and_paths_config.py`,
so it should trigger a Copilot review comment asking whether a dedicated path mapping is needed.
