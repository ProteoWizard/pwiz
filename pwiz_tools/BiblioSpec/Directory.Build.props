<Project>
  <!-- pwiz tool, not a Skyline project: chain to the repo-root Directory.Build.props instead of
       inheriting pwiz_tools/Directory.Build.props (Skyline's LangVersion/NoWarn/BinaryFormatter settings). -->
  <Import Project="$(MSBuildThisFileDirectory)..\..\Directory.Build.props" />
</Project>
