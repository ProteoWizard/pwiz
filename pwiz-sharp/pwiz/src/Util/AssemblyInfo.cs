// Marker source for Pwiz.Util. Real ports land alongside this file in subfolders
// (Chemistry/, Math/, Misc/). Remove once the first ported file is committed.

namespace Pwiz.Util;

/// <summary>Identification metadata for the assembly. Surface area is intentionally tiny —
/// the original file existed as a placeholder before any real port code landed and stuck around
/// because <c>ProjectName</c> shows up in version-stamping helpers.</summary>
public static class AssemblyInfo
{
    /// <summary>Stable assembly-name string used by version stamps and diagnostics.</summary>
    public const string ProjectName = "Pwiz.Util";
}
