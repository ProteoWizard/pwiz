---
description: Verify changes against critical rules
---
Read ai\CRITICAL-RULES.md, then review the current staged or modified files (`git diff` and `git diff --staged`) to verify changes comply with the project's critical rules.
